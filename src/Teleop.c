#pragma config(Hubs,  S1, HTMotor,  HTMotor,  HTMotor,  HTServo)
#pragma config(Sensor, S1,     ,               sensorI2CMuxController)
#pragma config(Motor,  mtr_S1_C1_1,     Left,          tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C1_2,     Right,         tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C2_1,     Hang,          tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C2_2,     LSlide1,       tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_1,     LSlide2,       tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_2,     Roller,        tmotorTetrix, openLoop)
#pragma config(Servo,  srvo_S1_C4_1,    Platform,             tServoStandard)
#pragma config(Servo,  srvo_S1_C4_2,    servo2,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_3,    servo3,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_4,    servo4,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_5,    servo5,               tServoNone)
#pragma config(Servo,  srvo_S1_C4_6,    servo6,               tServoNone)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//


#include "JoystickDriver.c"  //Include file to "handle" the Bluetooth messages.


void initializeRobot()
{
  // Place code here to sinitialize servos to starting positions.
  // Sensors are automatically configured and setup by ROBOTC. They may need a brief time to stabilize.

  return;
}

// Tank Drive
// Button 2 toggles front roller on and off

task main()
{
  initializeRobot();

  waitForStart();   // wait for start of tele-op phase

  bool rollerOn = false;

  while (true)
  {
	  motor[Left] = (joystick.joy1_y1 / 128) * 100;
	  motor[Right] = (joystick.joy1_y2 / 128) * 100;

	  getJoystickSettings(joystick);
    if(joy1Btn(2) == 1)
    {
    	if(!rollerOn)
    	{
    		motor[Roller] = 50;
    	}
    	else
    	{
    		motor[Roller] = 0;
    	}
    	rollerOn = !rollerOn;
    }
  }
}
